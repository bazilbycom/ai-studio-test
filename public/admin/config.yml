backend:
  name: github
  repo: your-github-username/your-repo-name # Replace with your actual repo
  branch: main
  base_url: https://your-oauth-proxy.com # Set up a proxy for GitHub OAuth if needed

local_backend: true # Allows local editing via npx decap-server

media_folder: "public/images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "blog"
    label: "Intelligence Reports"
    folder: "content/blog"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Publish Date", name: "date", widget: "datetime" }
      - { label: "Thumbnail", name: "thumbnail", widget: "image" }
      - { label: "Excerpt", name: "excerpt", widget: "text" }
      - { label: "Categories", name: "categories", widget: "list", default: ["Engineering"] }
      - { label: "Body", name: "body", widget: "markdown" }
